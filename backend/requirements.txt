fastapi>=0.115
uvicorn[standard]>=0.34
psycopg[binary]>=3.2
psycopg_pool>=3.2
pydantic>=2.7
python-dotenv>=1.0
requests>=2.32
anthropic>=0.40

# RAG — embeddings locales para el chat sobre informes de Contraloría
# sentence-transformers descarga ~500 MB la primera vez y consume ~400 MB RAM al cargar.
# El modelo se carga lazy (en la primera pregunta), no al startup de uvicorn.
sentence-transformers>=3.0.0,<4.0.0
