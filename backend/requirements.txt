fastapi>=0.115
uvicorn[standard]>=0.34
psycopg[binary]>=3.2
psycopg_pool>=3.2
pydantic>=2.7
python-dotenv>=1.0
requests>=2.32
anthropic>=0.40
openai>=1.50

# RAG — embeddings se generan vía HuggingFace Inference API (sin deps pesadas).
# Solo se necesita HF_TOKEN en .env (gratis en huggingface.co/settings/tokens).
# Si HF cae, fallback determinístico para no dejar muda la UI.
# Para indexar PDFs en local: pip install -r requirements-rag.txt aparte.
